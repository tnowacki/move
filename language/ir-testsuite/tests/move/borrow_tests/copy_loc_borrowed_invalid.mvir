module Tester {
    t() {
        let x: u64;
        let r1: &mut u64;

        x = 0;
        r1 = &mut x;
        // copy used to fail because of r1
        _ = copy(x);
        return;
    }
}

// check: Success
