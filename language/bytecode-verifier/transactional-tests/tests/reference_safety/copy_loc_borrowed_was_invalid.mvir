//# publish --address 0x1
module Tester {
    t() {
        let x: u64;
        let r1: &mut u64;

        x = 0;
        r1 = &mut x;
        // can copy even though data is "owned" by r1. bytecode verifier does not maintain ref trans
        _ = copy(x);
        return;
    }
}
