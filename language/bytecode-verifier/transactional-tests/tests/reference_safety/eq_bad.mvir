//# publish
module 0x1.Tester {
    eqtest1() {
        let x: u64;
        let r: &mut u64;

        x = 0;
        r = &mut x;
        // now valid! Can always read
        _ = copy(r) == copy(r);
        return;
    }
}

//# publish
module 0x1.Tester2 {
    eqtest2() {
        let x: u64;
        let r: &mut u64;

        x = 0;
        r = &mut x;
        // now valid! Can always read
        _ = copy(r) == move(r);
        return;
    }
}

//# publish
module 0x1.Tester3 {
    neqtest1() {
        let x: u64;
        let r: &mut u64;

        x = 0;
        r = &mut x;
        // now valid! Can always read
        _ = copy(r) != copy(r);
        return;
    }
}

//# publish
module 0x1.Tester4 {
    neqtest2() {
        let x: u64;
        let r: &mut u64;

        x = 0;
        r = &mut x;
        // now valid! Can always read
        _ = copy(r) != move(r);
        return;
    }
}
